# 이 파일을 .env.prod로 복사한 뒤 실제 값으로 채워서 EC2에만 두세요 (git에는 절대 커밋 금지).
# 강력한 랜덤 값 생성 예시:
#   비밀번호: openssl rand -base64 24
#   JWT 시크릿: openssl rand -base64 48

MYSQL_ROOT_PASSWORD=change-me-to-a-strong-password
JWT_SECRET=change-me-to-a-long-random-base64-string
